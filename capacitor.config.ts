import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vermeulen.jelle.brawlr',
  appName: 'brawlr',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
